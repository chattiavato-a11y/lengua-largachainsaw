# wrangler.toml  (Integrity Gateway) — minimal, no KV IDs or secrets here

name = "ops-integrity-gateway"
main = "src/worker.js"
compatibility_date = "2025-11-01"
workers_dev = true

# Bindings (KV/secrets/vars) are added via Cloudflare Dashboard only.
# Capabilities & capacities remain: AI binding, OPS_* KV namespaces, optional WebLLM R2 bucket,
# integrity vars, and escalation URL are all managed via dashboard bindings for efficiency.
